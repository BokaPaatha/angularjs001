//Write the functionality and send it back 
//Really lean packaging. Only a function. That too without a name. 

module.exports = function($scope) {
    $scope.message = 'There is some killer logic here.'
}